apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-vol-1-claim
  namespace: nginx
spec:
  volumeName: nginx-vol-1
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
